{
	"name": "fast-highlight",
	"version": "0.0.36",
	"main": "./src/FastHighlight.js",
	"author": "Hultsi",
	"description": "Allows easy code highlight & styling to .html files",
	"license": "MIT",
	"keywords": [
		"highlight",
		"codeblock"
	],
	"scripts": {
		"update-version": "node ./tools/update-version.js",
		"build": "npm pack && cd tests && npm install ../fast-highlight-0.0.36.tgz",
		"build-sh": "./tools/build.sh",
		"build-and-run": "npm pack && cd tests && npm install ../fast-highlight-0.0.36.tgz && node fast-highlight.js && git restore package-lock.json && cd .. && del fast-highlight-0.0.36.tgz"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/hultsi/fast-highlight.git"
	}
}